#lang sicp

; why is it reversed? because of the way you can append sth to list
; using cons - you can only append to the beginning, so in the end
; the result is reversed

; the second one - just wrong, even for [1] we get a wrong result - [[], 1]
